# Overnight progress

## Current direction: personal workspace, September 15

The user's daytime request supersedes the paid-service interface described in the historical overnight entries below. The active app is now a personal, local investigation chat with no checkout, access-key prompt, scope checkbox, order panel or public-library navigation. Local startup is the default, requires no administrator or billing credentials, and retains localhost/Host/origin guards. Legacy hosted APIs and private evidence are preserved; the obsolete scope-chat frontend was removed. The overnight automation remains paused and was not restarted.

Paste up to 20,000 characters of links, addresses and context. Generic intake recognizes supported explorer links and bare domains, preserves known token identity on ordinary follow-ups, and asks for ambiguous details. Website-only work starts immediately. Inputs received during a run are saved for one subsequent snapshot; old inputs remain fixed. New documentation is prioritized within the three-page limit while the first project link remains the domain anchor. Repeated links and report questions do not trigger fresh investigations.

Reports and real collector progress appear in the conversation. Expand the HTML report in place, inspect cited sources, or download a script-free personal report. Completion asks for a missing primary identity or GitHub link where appropriate. Follow-ups retrieve matching saved findings and retain citations and limits; missing identity does not block unrelated report questions. This is still deterministic intake and retrieval, not a connected general conversational model. Model-assisted report narration remains optional through the existing Pi API configuration.

New backend files: personal.ts, personal.test.ts, personal-intake.ts and personal-intake.test.ts. The server integrates the local router. SQLite persists conversations, request IDs and links to existing job/result storage. Duplicate delivery, lost responses, stale revisions, restart interruptions, retry ownership, pending input coalescing and per-conversation/global capacity are checked. A capped conversation cannot block other conversations.

The frontend was simplified in app.js and index.html with a separate personal.css and a small personal-report renderer option. Visual lock: existing Reducto reference d67d7c27-6e87-419c-a192-85c461259a4f owns the light canvas, serif display type, sharp borders and purple actions. Perplexity screens 9941f119-43a2-45ed-85ce-7f4f7a7c363d and bc0ec35b-a165-4e66-954b-2fbb19ff652e contribute the centered composer, inline progress and persistent follow-up input. History is a small menu rather than a public report sidebar. No new palette or decorative imagery was introduced.

Validation: strict TypeScript, all 194 tests and all four active client-module syntax checks pass. ESLint is not configured. Log: /tmp/mydevsaid-personal-final-check.log. Browser helper /tmp/mydevsaid-personal-browser.mjs exercises the real local HTTP router and SQLite with trusted fictional report fixtures: paste/start, input changes during active work, reload, two immutable runs, cited answers without rerunning, source focus, desktop/mobile geometry, HTML export, draft persistence, lost-response retry, history navigation, and new-chat drafts across another chat's reload. It also checks that navigation cannot send to the previous chat and that a newer draft survives retry/reload. Results: /tmp/mydevsaid-personal-browser-results.json. Default startup with no LOCAL_MODE, ADMIN_TOKEN, billing or model environment settings was separately verified.

A real NetNet investigation also completed through the new browser UI after a fresh official-address identity check. It generated 19 evidence records at 2026-09-15T13:15:57.120Z. Asking who controls the contracts returned a cited answer while the run count remained one. Live chat: http://127.0.0.1:3000/#chat/c3812725-b7dc-4055-922e-0f3893d03f19 . Private artifacts are data/evaluations/netnet/personal-netnet-chat.json and personal-netnet-report.html, with personal-chat screenshots. The deeper historical reviewed report remains separate; no claims from those manual probes were added to this automatic run.

Current main app PID 4999, exec session 57823, http://127.0.0.1:3000/ . It was restarted after the final changes, and the saved NetNet chat recovered with one completed run and five messages. Inspect processes before management. No deployment, external report publication, charge, purchased service, third-party contact or wallet transaction occurred. Next product work should address genuinely conversational interpretation through a configured model and more substantive reusable collectors, rather than bringing back the paid-service UI.


Deadline: September 15, 2026, 08:00 America/Toronto (12:00 UTC). Do not start new implementation after the deadline. The existing hourly heartbeat is `mydevsaid-overnight-iteration`.

## Accepted direction

Free, bounded text-based project scoping comes before a clear $100 scope confirmation and server-enforced payment gate. Deep analysis produces a categorized HTML report that a nontechnical reader can understand. Reports need repeatable evidence-backed diagrams for relationships, money flows and control, with text equivalents and explicit unknowns. Preserve purple/serif branding, the chat layout and public token report pages. Twitter is excluded.

## Run 1: discovery and Robinhood support

Verified at 03:11 UTC. `npm run check` passed strict TypeScript and 95 tests. ESLint is not configured. Browser checks at desktop and 390px confirmed Robinhood selection and no horizontal overflow; the mobile network field now has its own row.

- Added Robinhood mainnet (4663) across RPC collection, API intake/configuration, report names and UI. Actual RPC chain identity is checked. Mainnet/testnet mismatch and unsupported testnet intake have tests. Shared backend network mapping lives in `src/onchain.ts`.
- Added bounded Markdown/plain-text collection. Marked 18.0.5 tokenizes documentation without rendering or executing it. Tests cover reference links, code fences, unsafe URLs, byte limits and original evidence bytes.
- Discovery follows linked documentation candidates across origins within the existing page budget and DNS/redirect guards. It does not claim those domains share ownership. Empty static shells are disclosed as missing rendering coverage.
- Readable extracted text is retained with its link to the original captured bytes. Protocol assertions remain claims.
- Restarted the local server with current code. Node process was 74404, exec session 47474; inspect before managing it. A later text-only collector-method update requires another restart before the next API run.

## NetNet evaluation

Identity was confirmed from the project's official address list before the live run: NET `0xca9c78dd337a67f6e0077f65f5e9218719d30edf`, Robinhood chain 4663.

Sources: https://netnet.capital/, https://docs.netnet.capital/, https://docs.netnet.capital/official-channels. Official network reference: https://docs.robinhood.com/chain/connecting/.

Private evaluation files live in ignored `data/evaluations/netnet/`. `baseline-0.json` is the original website-only result. `iteration-1.json` is the new result. `iteration-1-access.json` contains the private order capability; do not display, publish or commit it.

| Measure | Original baseline | Iteration 1 |
| --- | --- | --- |
| Captured pages | 1 | 3 |
| Evidence items | 5 | 14 |
| Check procedures completed | 1 of 6 | 4 of 6 |
| Readable project pages | 0 | Docs index and official contract list |
| Chain/token supplied | None | Confirmed NET on 4663 |
| Mechanism explanation | Missing | Still inadequate |

Completed checks mean bounded procedures completed, not a safety verdict. Live iteration 1 recorded bytecode at block 63326989, Sourcify's exact runtime match plus matching stored bytecode, and 3 non-mint/burn transfers across 6 addresses in 128 blocks. This is not independent compilation or an organic-activity verdict. No public repository/API was discovered. Model narration is unavailable; the result is deterministic.

Local private order: `e404465d-6f90-4c42-b900-517f4f12489b`; draft report: `dd264b07-4878-4800-bdb9-5021b6fab94c`. No report was published and no payment was collected.

## Run 2: saved scope and a focused NetNet collection

Verified around 04:12 UTC. Strict TypeScript, 102 tests, and all four client module syntax checks pass. ESLint is not configured. The app server now runs PID 77268 in exec session 94139; inspect before managing it. This process includes the final backend and scoping copy changes.

Implemented persisted free guided scoping, capability-based recovery, a scope review card, explicit checkbox review, and the $100 checkout boundary. The composer stays available until checkout. Scope and submission snapshots are immutable after checkout; stale revisions and locked changes are rejected. Failed/concurrent checkout creation retries the same order. Paid failures cannot create another charge. The local evaluation path still requires administrator access. Browser checks covered multi-turn NetNet input, reload recovery with review reset, malformed token correction, previous-conversation recovery, and desktop/mobile scope review. At 390px, document width is 390px.

Five backend files: `src/scoping.ts`, `src/scoping.test.ts`, `src/store.ts`, `src/server.ts`, `src/server.test.ts`. Four UI files: `public/scope-chat.js`, `public/app.js`, `public/index.html`, `public/styles.css`. New static module is allowlisted. Design lock: Reducto canvas/purple actions/serif/sharp borders; chat composer continuity informed by ChatGPT; pre-payment scope visibility and recoverable errors informed by SSENSE checkout. Docs updated afterward.

Scoping limitations: parsing is deterministic, with no free crawling or model call; the UI says so. Concerns are saved as context but do not yet drive protocol-specific checks. `replace links` supports explicit replacement; network/token corrections request confirmation. Production direct submissions remain compatible and payment-gated, but bypass conversational review. Expired Stripe checkout replacement is not implemented. Free chats are limited to 30 per socket address/hour, so proxy users share that limit. Scope history recovery is local-browser capability based, not an account system.

### NetNet iteration 2

Reconfirmed NET identity from the official address list at 04:09:58 UTC, including Robinhood chain 4663. Recorded it in `iteration-2-identity.json`. Through the actual new scope/order API, selected these three pages: `https://docs.netnet.capital/mechanism`, `https://docs.netnet.capital/treasury`, `https://docs.netnet.capital/FEES.HTM`. The authenticated local run reached review without payment or publication.

Private ignored evaluation files: `iteration-2.json` contains the order and result. `iteration-2-scope-access.json` and `iteration-2-access.json` contain secret recovery capabilities; never display or commit them. Scope ID `bba62fab-69a7-451f-8875-a48b38cda405`, order ID `faf7b398-e8d1-4dd7-8de4-49f1cd42c3a1`, draft report ID `c698f436-049f-402f-b532-e76948f50a39`.

Result: 3 substantive pages (7,273 / 6,353 / 6,176 readable characters), 13 evidence items, 4 of 6 procedures complete. This improves source selection substantially over the first collection's homepage/index/address-list scope. The report explanation still only gives collection counts. The existing technical renderer and link map do not yet satisfy the nontechnical HTML report requirement.

Material documentation claims to evaluate next: NET stakes into rebasing sNET; new NET issuance depends on market premium and reserve limits; treasury reserves include USDG, protocol liquidity and a Morpho position; bond subscriptions and buybacks affect treasury flows; token trades incur a stated 5% fee; fee permissions are reportedly held by a Safe configured 1-of-1. These are project claims, not verified relationships. The primary token's simple owner/proxy checks do not verify the entire permission system or these connected contracts. The docs disclose a theoretical token-unit APY, which must not be presented as a promised dollar return. Captured sources can support a claim-labeled explanation and diagram while deeper contract checks remain explicit unknowns.

## Run 3: readable HTML reports and cited diagrams

Verified around 05:15 UTC. Strict TypeScript and 113 tests pass; all four client modules pass syntax checks. ESLint is not configured. Headless Chromium checks pass at 1360px and 390px: three NetNet diagrams, eleven claim-labeled relationships, no broken internal citations, no horizontal overflow, working source disclosures and HTML download, and no page errors. The exported report works with JavaScript disabled and contains no scripts. Text injection is escaped. A second fictional lending project, Juniper, passes the same renderer checks with seven categories and no invented diagrams or NetNet-specific content.

Added a strict presentation contract in `src/report-model.ts`: seven categories, cited statements, bounded graph data, known evidence references and explicit claim/observation/inference labels. The engine supplies a deterministic presentation; Pi may supply a validated presentation. Structural validation cannot prove source entailment. Deterministic source extraction does not invent semantic graphs. Model credentials remain unavailable.

The report now has a readable overview, concern cards, categorized explanations, numbered source links, three reusable diagram layouts with mobile/text equivalents, and progressive technical detail. Preparation time and source capture dates are distinct. The HTML export embeds styles, removes app-only controls and routes, retains source references and draft labels, and requires no scripts or external assets. No report was published and no payment was collected.

Backend/model phase: `src/report-model.ts`, `src/report-model.test.ts`, `src/engine.ts`, `src/pi-runner.ts`, `src/engine.test.ts`. Frontend phase: `public/report.js`, `public/report-map.js`, `public/report-map.test.js`, `public/styles.css`, `package.json`. A separate editorial agent prepared the private NetNet presentation, and the model agent independently checked source entailment. The requested artifact-design and dataviz skills were unavailable; this was disclosed, and existing design guidance plus browser verification were used.

### NetNet iteration 3: presentation evaluation

Reused iteration 2's captured evidence; this cycle made no new live collection. `iteration-3-editorial-presentation.json` contains an explicitly reviewed presentation with twenty statements and three diagrams covering staking, fee/treasury flows and permissions. All eleven relationships remain project claims. The report distinguishes token-unit APY from dollar returns, capacity-limited buybacks from guaranteed redemption, and documented Safe permissions from untested deployed configuration.

`iteration-3-reviewed.json` is a private editorial evaluation artifact, not an output from the unconfigured production Pi stage. `iteration-3-deterministic.json` is an actual harness replay using the captured collector fixtures: thirteen evidence items, seven categories and zero invented diagrams. Its prepared time is 05:11:45 UTC; its evidence is from iteration 2. That evidence includes one non-mint/burn transfer across two addresses in 128 blocks, ending at block 63363829. This small window cannot establish organic activity.

Private deliverables in ignored `data/evaluations/netnet/`: `netnet-report.html`, `report-desktop.png`, `report-mobile.png`, `diagram-desktop.png`, `diagram-mobile.png` and `report-browser-check.json`. A fictional generalization fixture is saved in `data/evaluations/juniper/report-fixture.json`. Access capability files remain private and must never be printed or committed.

Current app: PID 80259, exec session 2677, http://127.0.0.1:3000/. Current private preview: PID 79924, exec session 9431, http://127.0.0.1:3001/netnet-report.html. Inspect processes before managing them. The preview runs `/tmp/mydevsaid-report-preview.mjs`, binds loopback and serves an explicit allowlist. It can be restarted with Node. The browser-opening request was queued; do not assume the user has already seen it. Browser QA helpers: `/tmp/mydevsaid-report-browser-check.mjs` and `/tmp/mydevsaid-second-report-check.mjs`. Final check log: `/tmp/mydevsaid-report-final-check.log`.

## Run 4: related contracts and an automatic observed diagram

Verified around 06:15 UTC. Strict TypeScript and 136 tests pass; all four client modules pass syntax checks. ESLint is not configured. Final log: `/tmp/mydevsaid-related-final-check.log`. An independent reader reviewed the integration for payment/scope preservation and evidence overclaims, then checked the editorial report against its cited sources. No new material integration defects were found. A fee-capture timestamp was corrected to 04:10 UTC.

New scopes explicitly include up to four related addresses, pinned as `relatedContractLimit: 4`. The schema accepts only 0 or 4; omission means zero, preserving old scopes and CLI submissions. Saved paid scopes remain immutable. Existing production direct orders still bypass conversational review but remain payment-gated. No new live billing or publication was enabled.

Related candidates come from captured matching-chain explorer links. Text-only addresses are not assigned a chain. The inventory retains selected and excluded candidates, caps at 100, and discloses upstream truncation. Document context prioritizes Safe, treasury, staking and bond labels without treating those labels as verified roles. A regression test prevents a nearby mention of a Safe from promoting an unrelated credit vault.

Each related batch uses the primary token's block. It verifies actual chain ID and block number/hash before and after collection, with a maximum of 20 RPC requests. Per address it captures bytecode, owner(), getThreshold() and getOwners(). Owner arrays require canonical ABI encoding, 1 to 20 unique nonzero addresses and a threshold within the owner count. Missing bytecode is distinct from an empty-code response. Provider errors are sanitized. Any batch consistency failure discards its related observations. These views do not prove Safe implementation identity, authorization, signer independence, modules, guards or protocol permissions.

The deterministic report can now generate a cited approval-settings graph from validated related-contract observations. Its arrows run from the contract to its returned owner list and threshold. They describe returned data, not control of another module. Evidence medium, address, chain, block number/hash, bytecode and configuration are checked before graph generation. A 2-of-3 fixture guards against hardcoding NetNet. Mechanism and money-flow graphs still require a cited model or reviewed explanation.

Phases stayed below five files each. Collector agent: `src/related-contracts.ts` and its test. Root integration: `src/contract-inventory.ts`, its test, `src/collectors.ts`, `src/related-integration.test.ts`. Scope agent: `src/investigation.ts`, its test, `src/scoping.ts`, its test. Presentation phase: `src/control-presentation.ts`, its test, `src/report-model.ts`. Editorial artifact and docs were separate phases. Exa was unavailable; Safe interface signatures were checked against the official safe-global/safe-smart-account OwnerManager.sol source through a direct fetch. No fetched project code was executed.

### NetNet iteration 4: new live evidence

Reconfirmed NET identity and chain 4663 from the official address list at 06:04:04 UTC, saved as `iteration-4-identity.json`. Ran the actual runInvestigation harness with mechanism, treasury and official-channels as its three pages, with a four-address budget. The resulting private `iteration-4.json` was prepared at 06:08:57 UTC. It contains 18 evidence records, seven categories and one automatically generated diagram. Pi remains unconfigured; analysis is deterministic. This was an internal CLI/library evaluation, not a new checkout order.

At Robinhood block 63434034, the documented team Safe at `0x3bb7a23316f82c0e984fa2e784846d8928a35f42` returned a threshold of 1 and one owner address, `0xe7e867518c5b3d929ca63622f314ff9dc60e96f6`. It is an observed 1-of-1 interface setting, not proof of effective protocol permissions. Deployed bytecode was also returned for the documented Treasury `0x04822ea321a0dee6f40656172f29312104855d66`, Staking `0xb078cc304a0b264c5f3680dc0488954accd02e87` and GenesisBond `0x575b7b7c97ef3e21c82daeb427899d583e1e913f`. Generic owner probes established no supported owner value. NET's standard proxy slots were empty; this is not a permissionless/immutable verdict.

The latest activity query returned zero Transfer logs in 128 blocks, 63433907 through 63434034. An empty short window does not establish zero lifetime activity or organic usage. The address inventory retained 60 candidates and was marked truncated; four related addresses were selected and NET inspected separately. This is not an exact protocol-module count. Standard BondDepository, InverseBond, TaxCollector, Distributor and other listed modules received no contract calls in this run.

`iteration-4-editorial-presentation.json` contains 22 reader-facing items. `iteration-4-reviewed.json` combines the fresh observations with older captured fee evidence, retaining all capture dates. Its 31 evidence items support three graphs: staking and fees remain eight project-claim edges; the returned-settings graph adds two observed edges. The explanation is explicitly reviewed, not production Pi output. The old fee schedule is labeled 04:10 UTC; new sources and contract observations are around 06:09 UTC.

Private HTML deliverables: `data/evaluations/netnet/netnet-report.html` is the refreshed editorial example; `netnet-automatic-report.html` is the actual deterministic report. The prior editorial HTML is retained as `netnet-report-iteration-3.html`. Both exports work without JavaScript. Screenshots include updated report/diagram desktop/mobile files plus `approval-settings-desktop.png`, `approval-settings-mobile.png` and `scope-related-mobile.png`. Browser checks confirm no horizontal overflow at 1360px or 390px, no broken citations or page errors, working evidence navigation, escaped HTML text, automatic and editorial downloads, saved four-address scope recovery, review reset and an HTTP 401 for an unauthenticated deep-analysis request. No extra live collection was needed for these browser checks.

Current app: PID 82593, exec session 77068, http://127.0.0.1:3000/. Current private preview: PID 82825, exec session 29744. Editorial HTML: http://127.0.0.1:3001/netnet-report.html. Automatic HTML: http://127.0.0.1:3001/netnet-automatic-report.html. Dynamic automatic report: http://127.0.0.1:3001/automatic. Inspect processes before managing them. Preview script: `/tmp/mydevsaid-report-preview.mjs`. QA scripts: `/tmp/mydevsaid-related-browser-check.mjs` and updated `/tmp/mydevsaid-report-browser-check.mjs`; metric files are `related-browser-check.json` and `report-browser-check.json` in the ignored evaluation directory. No reports were externally published and no payments were collected in this run.

## Run 5: free website discovery in the scoping chat

Verified around 07:16 UTC. Strict TypeScript and 152 tests pass; all four client syntax checks pass. ESLint is not configured. Final log: `/tmp/mydevsaid-free-discovery-final-check.log`. A read-only integration review found no new material capability, payment, revision, budget or recovery defect. The existing direct-production-order conversational bypass remains unchanged and payment-gated.

The chat now saves the message/capability first, then automatically requests one free static-page lookup when it has links. The server reserves the one-batch budget before fetching. It attempts at most two pages, prioritizing supplied links over discovered docs, with four seconds, 100 KB and two redirects per page. No model, RPC, repository, domain history, API or activity collector runs. The response contains up to two source excerpts, six docs links, eight address candidates, capture dates and raw-byte hashes. It treats all extracted text and labels as unverified project claims.

The new capability-protected endpoint is POST `/api/scopes/:id/discovery` with the current revision. Reservations and completion each advance the saved revision. Concurrent requests share a job; the app caps active jobs at four. Cached results or failures cannot cause another lookup after reload or link replacement. Active lookup blocks edits and checkout. Interrupted reservations expire after 30 seconds without restoring the page budget. A late result cannot overwrite newer conversation state. Busy requests rejected before reservation can be retried manually. The UI retains recovery access, flags earlier starting links and resets the review checkbox.

The interface shows discovered material inside the chat even while the scope is incomplete. Documentation is added only when the user chooses it. A candidate button sends an explicit token/chain confirmation message; fetched data never silently sets either. Unsupported or unknown networks do not get a selection button. Composer drafts survive candidate/link button actions. Existing paid scopes and their inspection budgets remain fixed. Free discovery remains deterministic and is labeled accordingly.

Phases: discovery agent added `src/scope-discovery.ts` and its tests; root changed `src/scoping.ts`, `src/store.ts`, `src/server.ts` and added `src/scope-preview.test.ts`; UI agent changed `public/scope-chat.js`, `public/index.html`, `public/styles.css`. Each phase touched at most five files. Source files remain below 300 lines, but server.ts is now 282 lines: inspect size before future structural work. No new static asset was needed. The existing Refero/Reducto brand was retained.

### NetNet free-scope evaluation

Ran the actual browser against the local server with official-channels and mechanism as the two input pages. The fresh official address-list result again identified NET `0xca9c78dd337a67f6e0077f65f5e9218719d30edf` on Robinhood chain 4663 before the candidate was selected. The browser confirmed the scope stayed incomplete until that explicit action, then showed the reviewed four-related-address budget. No deep order or payment was created. The successful run used one two-page lookup; a preceding bounded trial exposed a Markdown-label formatting bug that was fixed before rerunning.

The first trial found that backticks around Markdown explorer links hid nearby document labels. Regression fixtures now cover code-formatted links, preceding addresses and table headers. Labels keep source context, fit within 100 characters and truncate at word boundaries. A final fixture replay using captured NetNet bytes produces clean NET, sNET, GenesisBond, ShareCertificate, Staking, Treasury, Distributor and BondDepository labels. Its two raw source hashes match the later live free-preview hashes. The replay is labeled as a fixture, not another live collection.

Browser checks passed at 1360px and 390px with no horizontal overflow or page errors. They covered explicit token selection, adding docs without refetching, draft retention, reload recovery without another batch, checkbox reset, earlier-link labeling and escaped untrusted HTML text. The final label cleanup was checked through a captured-fixture browser replay with zero POST requests.

Private artifacts under ignored `data/evaluations/netnet/`: `iteration-5-free-scope.json` contains the successful captured conversation; `iteration-5-free-scope-access.json` contains its private capability and must never be displayed or committed; `iteration-5-free-replay.json` contains the final captured-byte replay. Screenshots: `free-discovery-desktop.png`, `free-scope-mobile.png`, `free-token-candidate-desktop.png`, `free-token-candidate-mobile.png`, plus final clean-label `free-discovery-replay-desktop.png` and `free-token-candidate-replay-mobile.png`. Metrics: `free-discovery-browser-check.json`. Browser helpers: `/tmp/mydevsaid-free-discovery-browser.mjs` and `/tmp/mydevsaid-free-preview-fixture-browser.mjs`. The browser test deliberately changed the saved scope's links after capturing its artifact to verify stale-preview labeling; do not assume the recovered server scope still has the artifact's original links.

Current main app: PID 85658, exec session 85882, http://127.0.0.1:3000/. It includes the final label cleanup. The private report preview remains PID 82825, exec session 29744, at http://127.0.0.1:3001/. Editorial and automatic HTML report links from Run 4 remain valid; this run did not recollect contracts or alter their evidence. Inspect processes before managing them. No external publication, paid services, wallet actions or contacts occurred.

## Run 6: visible contract scope and a source-backed wallet follow-up

Verified around 08:19 UTC. Strict TypeScript and 158 tests pass; all five client modules pass syntax checks. ESLint is not configured. Final log: `/tmp/mydevsaid-inventory-final-check.log`. An independent integration review found no material inventory, snapshot, export or rendering defect. The report inventory was checked in Chromium at 1360px and 390px, including its offline export. The running main app includes the new asset route.

The new `public/report-inventory.js` helper presents primary, selected and excluded address candidates. It validates inventory shape, evidence identifiers and chain/address/block metadata before assigning read coverage. Missing reads, unavailable code and an empty-code result remain distinct. Document names remain claimed roles. The NetNet example shows four selected read attempts, the primary token separately and 55 excluded candidates, with a visible truncation notice. This is not a total protocol-module count. Six tests cover malformed and mismatched captures, ambiguous evidence IDs, canonical owner settings and bounded untrusted labels.

Frontend phase: `public/report-inventory.js`, its test and `public/styles.css`. Integration phase: `public/report.js` and `src/server.ts`. HTML exports retain the inventory and native excluded-address disclosure. Their raw-evidence truncation message now states that only the first 20,000 characters are embedded, instead of implying the offline file contains the complete JSON. Server and report modules remain under 300 lines. The documentation phase updates this log and README.

### Separate private source study

Reconfirmed official NET identity on Robinhood 4663 before the read-only follow-up. Captured Sourcify records for the documented Safe proxy and TaxCollector, saved under ignored `data/evaluations/netnet/source-study-6/`. The Safe proxy record matched the earlier block 63434034 bytecode. An attempted historical state read was unavailable; the cause was not established. A fresh pinned snapshot at block 63504974, hash `0x6ae6954790120ba98ed7e673c22b147ce37bfc7975916455416cc256100e5d57`, captured at 08:08:58 UTC, used nine read-only RPC requests including chain/block consistency checks.

That snapshot matched the proxy code, read singleton slot zero as `0x29fcb43b46531bca003ddc8fcb67ffe91900c762`, captured its deployed code, and returned one listed owner with a threshold of one. A full singleton source request exceeded the collector byte limit. A smaller Sourcify metadata/ABI record at 08:12:47 UTC returned `exact_match`, identified `contracts/SafeL2.sol:SafeL2`, and its stored runtime matched the fresh singleton code. This establishes code correspondence, not an independent compilation, source audit, signer identity, effective fee permissions, or coverage of modules and guards. The old `study.json` predates this successful implementation lookup; use the fresh observation and metadata files for the latest finding.

TaxCollector source remains study material, outside the report's upgraded findings. It contains immutable destinations and one-time deployer wiring rather than owner(). Its implementation uses a 400-bps initial team allocation; an interface comment still says 300. The executable source and stale comment must be distinguished. TaxCollector bytecode, current wiring, destinations, allocations and actual fee transfers have not been compared with live state. Do not infer that a missing owner() means no privileges.

### NetNet iteration 6: reviewed report

`iteration-6-reviewed.json` combines the earlier report with four new validated evidence records: fresh wallet RPC, proxy source, singleton verification metadata and fresh official identity. It contains 35 evidence records and one additional bounded code-correspondence finding. `iteration-6-editorial-presentation.json` supplies the reviewed prose. `iteration-6-review-notes.json` records evidence IDs and validation. These private probes and editorial changes are not automatic production harness output and do not expand any existing paid order. An independent reader verified both runtime comparisons and original source hashes, checked the new finding and diagram against their evidence, and found no material correction needed.

The three diagrams now contain eight project-claim relationships for staking/fees and four observed wallet relationships: returned owner count, returned threshold, implementation slot and runtime correspondence. The prose distinguishes fee documentation captured at 04:10 UTC, the broader inventory/activity at 06:09 UTC and the wallet snapshot at 08:08 UTC. The fresh wallet check does not refresh the earlier data. Effective protocol permissions and financial behavior remain unverified.

Both refreshed HTML artifacts passed browser checks with no page errors, broken citations or horizontal overflow. The reviewed report contains three diagrams and 35 source records; its standalone export works with JavaScript disabled. Private deliverables remain `netnet-report.html` for the reviewed example and `netnet-automatic-report.html` for the actual Run 4 deterministic result, now with the improved inventory renderer. `netnet-report-iteration-4.html` preserves the previous reviewed export. Inventory screenshots use `inventory-reviewed-1360.png`, `inventory-reviewed-390.png`, `inventory-automatic-1360.png` and `inventory-automatic-390.png`; metrics are in `inventory-browser-check.json` and the refreshed `report-browser-check.json`. The new wallet diagram was visually inspected in `implementation-diagram-desktop.png` and `implementation-diagram-mobile.png`.

Current main app: PID 87764, exec session 3428, http://127.0.0.1:3000/. Current private preview: PID 88016, exec session 69044, http://127.0.0.1:3001/. Inspect processes before managing them. Preview helper `/tmp/mydevsaid-report-preview.mjs` serves iteration 6 at `/` and the unchanged automatic record at `/automatic`; it allowlists the new inventory module. Browser helpers are `/tmp/mydevsaid-report-browser-check.mjs` and `/tmp/mydevsaid-inventory-browser-check.mjs`. The latter uses only local captured records. No publication, payment, purchased services, contacts or wallet transactions occurred.

## Run 7: fee configuration and checkout recovery

Verified around 09:14 UTC. Strict TypeScript and 166 tests pass; all five client modules pass syntax checks. ESLint is not configured. Final log: `/tmp/mydevsaid-checkout-final-check.log`. Checkout browser fixtures covered seven cases without live Stripe traffic: replacement, completed checkout, funded order, provider failure, foreign destination, insecure destination and wrong order. They also checked duplicate clicks, capability headers and stale-link removal. The reviewed and automatic reports passed desktop/mobile, citation and script-free export checks again.

### Expired checkout recovery

The backend now retrieves a checkout's authoritative Stripe state when the user resumes it. Open sessions are reused. Only expired, unpaid sessions allow replacement. Completed or paid sessions wait for the signed webhook and expose no checkout URL. Provider state does not fund work. Order identity, amount, currency, generation and HTTPS destination checks reject mismatches. The original generation-zero request and idempotency key remain compatible with earlier attempts.

The store reserves a generation before network creation. Failed creation and restart reuse it. Replacements keep the same immutable order and scope. Up to eight prior session IDs are retained so a late valid paid webhook can still fund its own order; replay cannot queue twice. A payment arriving during replacement creation wins, and funded responses never expose the new URL. Eight replacements are allowed; an uncertain creation attempt older than 23 hours requires operator review before paying. These permanent conditions return safe, actionable 409 responses; provider errors remain sanitized 502s. Historical checkout IDs and internal retry fields are withheld from customer responses.

New endpoint: POST `/api/orders/:id/checkout`, using the existing order bearer capability. Repeating scoped POST `/api/orders` uses the same recovery path. Both share a limit of six provider jobs per order per minute; concurrent callers reuse the active job. An independent review caught the initial missing sequential-request limit. It was fixed centrally and regression-tested with a mocked clock: six reads, seventh request rejected before provider work, then window expiry restores access without creating or funding anything. The reviewer confirmed the fix and found no additional material issue.

The order panel now uses a recovery button instead of a cached Stripe link. It validates response identity, only redirects an unpaid awaiting-payment order, rejects nonstandard ports and unsafe destinations, coalesces clicks and ignores stale responses when another order becomes active. Funded orders lose checkout controls. Recovery failures remain actionable. Screenshot: `checkout-recovery-mobile.png`; metrics: `checkout-browser-check.json`; browser helper: `/tmp/mydevsaid-checkout-browser-7.mjs`. These use intercepted local fixtures, never live Stripe.

Backend phase touched five files: `src/payments.ts`, `src/store.ts`, `src/server.ts`, `src/checkout-recovery.test.ts` and a small fixture update in `src/server.test.ts`. Frontend phase changed `public/app.js` and `public/report.js`; a separate two-file review fix added the central provider-request limit and its test. Documentation updates are a later two-file phase. Server is now 294 lines, report renderer 288, app 265: inspect size before future structural work.

### NetNet TaxCollector follow-up

The private script `/tmp/mydevsaid-tax-study-7.mjs` reconfirmed NET and TaxCollector identities on the current official address list, captured the fee document, then used 17 bounded read-only RPC requests. It compared TaxCollector bytecode against the previous captured Sourcify record, read twelve no-argument views and rechecked chain/block consistency. Function selectors were calculated with installed Foundry `cast`, not guessed; no fetched source was executed and no transaction was sent.

Snapshot: Robinhood chain 4663, TaxCollector `0x086c58400b8708ef993f256e12e752dcf0ac918e`, block 63537619, hash `0x70068a7e2b9ba4cb6db3fc976dcd965ba1d94b908a0699e7581f45ee083f3763`, captured around 09:04 UTC. All twelve views returned supported values. NET, Treasury and team Safe addresses match the documented identities. `wired()` returned true, `teamBps()` returned 0 and `treasuryBps()` returned 500. The matching executable source divides conversion output using a denominator of 500, supporting a configured 0% management / 100% treasury allocation at this snapshot. This does not establish a completed conversion, actual revenue, treasury ownership, enforcement of a 5% trading fee or future behavior.

Private captures: `tax-study-7/identity.json`, `fees.json` and `observation.json`. Source remains `source-study-6/tax-collector.json`; its old study notes about untested wiring are superseded by this new bounded snapshot. Other module behavior remains unchecked. An independent reviewer verified the raw source hash, runtime comparison, all twelve selectors and raw RPC results, denominator arithmetic and matching destination labels.

`iteration-7-reviewed.json` adds four validated evidence records and one bounded configuration finding to Run 6. It now contains 39 evidence records. `iteration-7-editorial-presentation.json` and `iteration-7-review-notes.json` record presentation and provenance. The three diagrams contain five claimed and seven observed relationships: staking claims, a fee diagram separating two claimed routes from three returned settings, and the four observed wallet relationships. The report distinguishes the 09:04 TaxCollector snapshot, 08:08 Safe check and 06:09 automated inventory. The inventory section now explicitly describes the original automated collection, so its excluded candidates are not confused with separately described private follow-ups.

The latest reviewed HTML is `netnet-report.html`; `netnet-report-iteration-6.html` preserves the prior export. `netnet-automatic-report.html` remains the actual Run 4 deterministic result with current rendering. Neither private follow-up is claimed as automated production output. The new fee diagram was visually inspected at desktop and mobile sizes in `fee-diagram-desktop.png` and `fee-diagram-mobile.png`. Existing report and inventory browser metrics are refreshed. No external publication, payment, purchased service, third-party contact or wallet transaction occurred.

Current main app: PID 90450, exec session 92734, http://127.0.0.1:3000/. Current private preview: PID 89905, exec session 25030, http://127.0.0.1:3001/. Preview `/` serves iteration 7; `/automatic` serves the actual automatic record. Inspect processes before managing them. The app remains authenticated local mode, with billing and model credentials unavailable. The overnight automation is still active.

## Run 8: required production scoping and scannable findings

Verified around 10:10 UTC. Strict TypeScript and 170 tests pass; all five client modules pass syntax checks. ESLint is not configured. Final log: `/tmp/mydevsaid-scope-gate-final-check.log`. Independent review found no material regression in intake, payment, recovery, report evidence, native disclosures or export. No new live NetNet collection ran in this pass.

### Production intake

New production POST `/api/orders` requests now require a saved conversation scope with its capability and current revision. Direct link/token payloads are rejected even with an administrator bearer token, before URL validation, order creation, provider calls or queue entry. Scoped payloads accept only scope ID, capability and revision; client-supplied replacement links or tokens are rejected. Draft scopes and active free lookups cannot enter checkout. Locked scopes with the same revision still resume the existing order.

Authenticated local development retains direct intake for private harness evaluations. Existing legacy orders retain capability reads, checkout recovery and signed-webhook payment handling. Privileged report rechecks remain an internal administration path. These changes do not alter checkout generations, rate limits, immutable scopes or the payment gate.

Backend phase touched three files: `src/server.ts`, `src/server.test.ts` and new `src/scope-gate.test.ts`. Four new tests cover unscoped production requests, incomplete/stale/unauthorized/client-mutated scopes, a valid immutable scoped order with locked-scope retry, and local/legacy compatibility. Existing successful production fixtures now explicitly create a scope instead of relying on direct intake.

### Finding rows and keyboard access

The checked-claims section uses native expandable rows. Each row preserves the original claim, status, severity and category; opening it reveals the explanation, impact and nested source/limit disclosure. A four-label guide explains supported, partially supported, contradicted and unverified without turning them into a project-wide verdict. Summary content uses phrasing elements rather than nested interactive controls. HTML export retains these native interactions with JavaScript disabled. Section navigation now moves focus to the target section without changing the application route. Source links continue to open and focus their captured evidence.

The visual lock remains Reducto: serif display type, light canvas, sharp borders and restrained purple. This pass re-read Refero's Reducto style `d67d7c27-6e87-419c-a192-85c461259a4f` and Mapbox Feedback Explorer screen `7e8ee64e-90bd-42b7-865d-bb69ea38b8f6`; only the scan-friendly separation of description and status was adapted from Mapbox. No new dashboard shell, palette, score or pagination was introduced. Frontend phase changed `public/report.js` and `public/styles.css`. Documentation updates are a separate two-file phase.

Browser checks covered NetNet and a new fictional Juniper fixture through the actual deterministic harness. Juniper exercises all four result statuses, seven report categories and honest missing diagrams, without NetNet or TaxCollector content. Both fixtures passed keyboard section navigation, Enter-to-expand behavior, nested evidence links and focus, 1360px/390px layout, and native offline HTML disclosures. Private fixture: `data/evaluations/juniper/iteration-8-fixture.json`. Screenshots: `finding-rows-1360.png` and `finding-rows-390.png` in each project's evaluation directory. Metrics: `data/evaluations/netnet/findings-browser-check.json`. Helper: `/tmp/mydevsaid-findings-browser-8.mjs`.

The reviewed NetNet result remains iteration 7: 39 evidence records, three diagrams, five claimed relationships and seven observed relationships. This pass changes rendering, not the underlying evidence or verification statuses. Both `netnet-report.html` and `netnet-automatic-report.html` were re-exported and checked; `netnet-report-iteration-7.html` preserves the previous rendering. The automatic report remains the actual Run 4 result; private source/fee follow-ups remain separately labeled reviewed work.

Current main app: PID 92237, exec session 2490, http://127.0.0.1:3000/. Private report preview remains PID 89905, exec session 25030, http://127.0.0.1:3001/. Inspect processes before managing them. Server is 296 lines, report renderer 294, stylesheet 290: follow the cleanup rule before any future structural refactor once a file exceeds 300 lines. The app remains local-only, with model and live billing unavailable. No publication, charge, purchased service, contact or wallet transaction occurred. The overnight automation remains active.

## Run 9: scoped domain registration and fresh automatic NetNet result

Verified around 11:10 UTC. Strict TypeScript and all 175 tests pass; ESLint is not configured. Log: `/tmp/mydevsaid-domain-final-check.log`. A read-only independent review found no material issue in legacy-scope compatibility, suffix handling, RDAP response identity, privacy filtering or bounded collection. The scope phase touched four files (investigation/scoping and their tests); the collector phase touched four files (collector, collector tests, package manifest and lock). Documentation is this separate two-file phase.

New or explicitly revised free scopes pin `domainLookup: "registrable_domain"` and display one registration lookup for the first supplied link's registrable domain. Locked old scopes retain their original policy; omitted means legacy exact host with the historical leading-www removal. The collector uses pinned tldts 7.4.13 with its bundled Public Suffix List, skips known private hosting, special-use names, IP addresses and non-registrable/unknown suffixes, and makes at most one logical RDAP lookup through the existing guarded redirect path. It requires matching domain identity, strips registrant contact entities, and records the submitted host, queried domain, policy and parser version. Parent-domain registration does not establish project/subdomain age, ownership or safety; shared hosting missing from the suffix list remains a limitation.

Tests cover multi-label suffixes, IDNA, trailing dots, private hosting, reserved domains, IPs, mismatched responses, legacy lookup behavior and locked-scope compatibility. The first run correctly skipped reserved example.com, exposing a test-fixture mistake; fixtures were corrected to ordinary registrable names and reserved-name exclusion is now explicit.

One new automatic NetNet investigation ran after a fresh official-address identity check. `iteration-9.json` is the actual deterministic harness output, generated 11:07:42 UTC, with 3 pages, 19 evidence records and one observed approval-settings diagram. `iteration-9-automatic-record.json` wraps it for the renderer. `iteration-9-identity.json` records the separate identity preflight, which is not counted as report evidence. The scope explicitly includes the new domain policy and the existing four-related-address budget. No paid order was changed.

The docs.netnet.capital input now resolves to an RDAP lookup for netnet.capital. The registry reports registration at 2026-07-14T05:34:16.649Z; the report labels this as domain registration only. On-chain observations pin block 63610643, hash 0x902faffb4e019a98faef2649bca8fa765155985893dbe1625271be24a50a0928. The selected Safe-compatible contract still returned one required approval and one owner. Four selected related addresses returned code; no new generic owner result was established. There were zero counted transfers in blocks 63610516 through 63610643. Repository and API coverage remain blocked. The automatic run does not perform the private Safe implementation or TaxCollector-specific checks from earlier reviewed work.

The private reviewed result remains iteration 7, with 39 evidence records and three diagrams. Its snapshot dates and reviewed label remain explicit. `netnet-report.html` exports that reviewed result. `netnet-automatic-report.html` now exports the actual Run 9 output; `netnet-automatic-report-iteration-4.html` preserves the prior automatic export. The preview `/automatic` and automatic JSON routes now serve Run 9. No reviewed follow-up was relabeled as automatic.

Browser checks passed for reviewed and automatic inventories, 1360px/390px layouts, finding and evidence disclosures, keyboard focus, all four finding statuses in the fictional Juniper fixture, diagram geometry and citations, escaping, and script-free offline export. The new expanded domain finding was checked at both widths and visually inspected on mobile; screenshots are `domain-finding-1360.png` and `domain-finding-390.png`. Existing report/inventory/finding metrics and both HTML exports were refreshed. The reviewed diagrams still have five claim edges and seven observed edges.

Current app PID 94178, exec session 27398, http://127.0.0.1:3000/. Private preview PID 94110, exec session 35698, http://127.0.0.1:3001/. Inspect processes before managing them. Billing and model credentials remain unavailable; the app is authenticated local mode. No publication, charge, purchased service, contact or wallet transaction occurred.

## Morning handoff

Implementation passes are complete. At the 08:00 Toronto deadline, check the app and both HTML artifacts without starting more implementation. Deliver a concise summary with links to the app, reviewed NetNet report, and actual automatic report. State that strict TypeScript and 175 tests passed, plus desktop/mobile and offline HTML checks. Explain that reviewed Safe/fee probes exceed the current automatic collector, while model narration and live billing still need configuration and testing. Full semantic API tests, independent source compilation, comprehensive permissions and economic correctness, common gas funder tracing, and continuous monitoring remain gaps. Do not claim exhaustive verification or a launch-ready paid service. Pause the existing overnight automation after the final handoff; do not create another automation.

The overnight automation remains active until 08:00 Toronto. At or after that time, summarize and pause it; start no further implementation.
